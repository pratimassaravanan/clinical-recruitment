# Planned Method Integrations

This directory is the research-side staging area for paper-inspired long-horizon agents that should remain separate from the serving path.

Current status:

- `HCAPO`: planned
- `MiRA`: planned
- `KLong`: planned
- `Plan-and-Act`: scaffolded at the runtime level through planner/executor-oriented baseline logic
- `MemexRL`: planned

These methods are not claimed as implemented benchmark results yet. The current offline results are produced by local benchmark-specific baselines in `research/policies.py`.
